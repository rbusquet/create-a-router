{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Jest in open file",
            "type": "shell",
            "command": "node --inspect-brk node_modules/.bin/jest --runInBand ${relativeFile}",
            "problemMatcher": []
        }
    ]
}